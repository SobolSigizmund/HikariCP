addons:
  artifacts:
    debug: true
    paths:
    - libs.zip
    - build.zip
    - testbuild.zip
    target_paths: bb145997-687f-4b09-ab1e-5fe84ecc1423
after_success:
- ./df-build.sh
cache:
  directories:
  - $HOME/.m2/repository
install: /bin/true
jdk:
- oraclejdk8
language: java
notifications: {}
script:
- mvn install -Dskip.unit.tests=true -Dmaven.javadoc.skip=true -V -B
- mvn clean test coveralls:report -V -P coverage
- jdk_switcher use openjdk7
- mvn test
sudo: false
# Modified by Insights Service at 2016-05-10 16:26:03.544926
